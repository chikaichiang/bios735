% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bino_reg.R
\name{beta1der}
\alias{beta1der}
\title{First Derivative of the Log-Likelihood (Score Function)}
\usage{
beta1der(X, Y, beta.t, n)
}
\arguments{
\item{X}{A matrix of covariates}

\item{Y}{A numeric vector of counts of "successes"}

\item{beta.t}{A numeric vector of current coefficient estimates}

\item{n}{A numeric vector of binomial denominators (trials)}
}
\value{
A numeric matrix representing the gradient vector
}
\description{
Computes the gradient of the log-likelihood function for binomial regression.
}
\examples{
# Using mtcars dataset
data(mtcars)

# Create design matrix and response
X <- model.matrix(~ hp + wt + cyl, data = mtcars)
Y <- mtcars$vs  # engine type (0 = V-shaped, 1 = straight)
n <- rep(1, nrow(mtcars))  # Bernoulli trials

# Compute score at initial parameters
beta_init <- rep(0, ncol(X))
score <- beta1der(X, Y, beta_init, n)
head(score)
}
